# Git Flow

- Main branch: `main`.
- Feature branches: `feature/<short-name>`.
- Pull requests merge into `main`.
- Do not push directly to `main`.
- CI must pass before merge.
- Release branches and release tags can be added later.
